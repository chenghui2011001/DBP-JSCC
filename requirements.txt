# Reference pip package set from the `farGan-sota` environment.
# Python version in that environment: 3.10.18
#
# For the exact Torch CUDA build used locally:
#   pip install --extra-index-url https://download.pytorch.org/whl/cu118 -r requirements.txt
#
# Local source trees such as `mamba/` and `mamba_ssm/` are kept in-repo and
# may still require separate editable install / CUDA compilation depending on
# the target machine.

--extra-index-url https://download.pytorch.org/whl/cu118

torch==2.7.1+cu118
torchaudio==2.7.1+cu118

numpy==1.26.4
scipy==1.13.1
numba==0.61.2

librosa==0.11.0
soundfile==0.13.1
soxr==1.0.0

matplotlib==3.8.3
pandas==2.3.3
tqdm==4.67.1
tensorboard==2.15.2

einops==0.8.1
hydra-core==1.3.2
omegaconf==2.3.0
PyYAML==6.0.1

torchcrepe==0.0.24
transformers==4.56.2
wandb==0.21.4

pesq==0.0.5
pystoi==0.3.3
pyvisqol==0.0.2

scikit-learn==1.7.2
